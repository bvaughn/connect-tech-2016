const markup = `function cellRendererFixed ({ columnIndex, key, rowIndex, style }) {
  return (
    <div
      key={key}
      style={style}
    >
      {index}
    </div>
  );
}`;

export default markup;
