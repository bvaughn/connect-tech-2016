const markup = `function renderCollection (props) {
  return (
    <Collection
      cellCount={itemsArray.length}
      cellRenderer={cellRenderer}
      cellSizeAndPositionGetter={cellSizeAndPositionGetter}
    />
  )
}`;

export default markup;
